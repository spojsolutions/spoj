#include <bits/stdc++.h>
using namespace std;
int main(){

	int t;
	cin>>t;
	while(t--){

		int n , m;
		cin>>n>>m;

		pair<int , int > arr[m+9];
		for(int i = 0 ; i < m ; i++)
			cin>>arr[i].first>>arr[i].second;

		sort(arr , arr + m);

		int a = arr[0].first , b = arr[0].s
		while(true){


		}
	}
}